{
  "version": 2,
  "builds": [
    { "src": "server.js", "use": "@vercel/node" }
  ],
  "routes": [
    { "src": "/publics/(.*)", "dest": "/publics/$1" },
    { "src": "/(.*)", "dest": "server.js" }
  ]
}